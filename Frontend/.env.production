# Production Environment Variables
# Bu dosya production build sırasında kullanılır

# API Base URL - Production'da API endpoint'iniz
# Same-origin deployment için boş bırakabilirsiniz (varsayılan: /api)
VITE_API_URL=/api
